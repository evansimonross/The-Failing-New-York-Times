<h5>{{name}}
  {{#if self}}
  <span id="logout">&nbsp;&nbsp;<a href="#">(Logout)</a></span>
  {{/if}}
</h5>
<ul>
  {{#each comments}}
  <li><a href="/articles/{{article}}">{{text}}</a></li>
  {{/each}}
</ul>